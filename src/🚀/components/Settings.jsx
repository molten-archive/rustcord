import modules from "../modules";
const React = modules.React

function Settings(props) {
    console.log(props)
    return (
        <>
        rusty cord
        </>
    )
}

export default Settings;